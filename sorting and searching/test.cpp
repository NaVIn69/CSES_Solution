#include<bits/stdc++.h>
using namespace std;
#define read(arr) for(auto v:arr) cin>>v;
int main(){
    int n;
    cin>>n;
    int arr[n]={0};
    read(arr);
    for(auto v:arr) cout<<v<<" ";
}